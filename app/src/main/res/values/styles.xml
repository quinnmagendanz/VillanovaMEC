<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/novaDark</item>
        <item name="colorPrimaryDark">@color/novaBlack</item>
        <item name="colorAccent">@color/novaBlue</item>
    </style>

</resources>
