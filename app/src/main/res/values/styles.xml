<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/novaBlue</item>
        <item name="colorPrimaryDark">@color/novaSubBlue</item>
        <item name="colorAccent">@color/novaWhite</item>
    </style>

</resources>
